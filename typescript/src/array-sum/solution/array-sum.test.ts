import { expect, describe, it } from "vitest";

import { arraySum } from "./array-sum";

describe("Array sum", () => {
  it.todo("handles no input");

  it.todo("handles a single number in the array");

  it.todo("handles 2-4 nummbers in the array");

  it.todo("handles 100 numbers in the array");

  it.todo("handles negative numbers (odd and even array length)");

  it.todo("handles decimals");

  it.todo("gracefully dies when exceeding maximum possible value");
});
